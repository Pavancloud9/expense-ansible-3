- name: configure frontend
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: installed
  
  - name: start nginx service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: true